# Public URL, not a secret — committed so every build/deploy picks it up
# without a manual env export. See Navbar.tsx's PORTFOLIO_URL constant.
VITE_PORTFOLIO_URL=https://luanncurioso-portfolio.pages.dev/
