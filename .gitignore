# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies (root + every workspace package/app)
node_modules/
.pnp
.pnp.js
# Leaked in here once from a Docker/pnpm interaction during Phase 11
# testing — pure rebuildable cache, never meant to be tracked.
.pnpm-store/

# testing
coverage/
playwright-report/
test-results/

# production
dist/
build/

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude Code working files (personal, not part of the project)
.claude/
ROADMAP.md
