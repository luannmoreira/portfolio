{
  "name": "luannmoreira-dev",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev:portfolio": "pnpm --filter portfolio dev",
    "dev:blog": "pnpm --filter blog dev",
    "build:portfolio": "pnpm --filter portfolio build",
    "build:blog": "pnpm --filter blog build",
    "preview:portfolio": "pnpm --filter portfolio preview",
    "preview:blog": "pnpm --filter blog preview",
    "lint:portfolio": "pnpm --filter portfolio lint",
    "lint:blog": "pnpm --filter blog lint",
    "deploy:portfolio": "pnpm --filter portfolio build && wrangler pages deploy apps/portfolio/dist --project-name luanncurioso-portfolio --branch main",
    "deploy:blog": "pnpm --filter blog build && wrangler pages deploy apps/blog/dist --project-name luanncurioso-blog --branch main",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "wrangler": "^4.114.0"
  }
}
